>git  init[to initialize git]

>git status [to list all untracked files]

>git add -A[to adding all untracked files to staging area]

>git commit -m"commit messege"[to commit added file to local repository]

>git clone[to take exact copy from remote repository to local workspace]

>git pull[to take changes from remote repsitory to local repository]

>git push[to sent changes from local workspace to remote repository]

>git config--list[to list configuration]
>giy config--global user.name"username"[to add usernmqe]
>git config--global user.email"useremail"[to add useremail]